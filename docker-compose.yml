version: "3.9"

services:
  app:
    build: .
    env_file:
      - .env.production
    ports:
      - "3000:3000"
    restart: unless-stopped
    depends_on:
      # Uncomment if you want to run MySQL in the same compose stack
      # - db
      []
    # Uncomment to mount logs or volumes if needed
    # volumes:
    #   - ./logs:/app/logs

  # db:
  #   image: mysql:8
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: change-me
  #     MYSQL_DATABASE: marqlet
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - db_data:/var/lib/mysql

# volumes:
#   db_data:
